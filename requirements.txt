# Pinned so the local venv matches what's been tested for this course.
# Use Python 3.9-3.11 — see 00-setup/README.md for a confirmed Python 3.12 worker-crash
# issue with this PySpark version on Windows.
pyspark==3.5.3
pytest==9.1.1
chispa==0.12.0

# Added for Module 08 (Pandas UDFs) — pandas_udf/applyInPandas need pyarrow for
# Arrow-based columnar transfer between the JVM and the Python worker.
pandas==3.0.3
pyarrow==25.0.0

# Added for Module 11 (Delta Lake) — pinned to the Delta 3.x line, which targets
# Spark 3.5.x (Delta 4.x requires Spark 4.0). First use needs internet access to
# resolve the Delta JARs from Maven Central via Ivy (cached under ~/.ivy2 after).
delta-spark==3.3.0
